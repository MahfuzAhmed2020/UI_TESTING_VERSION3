package com.osa.tetes;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.testng.Assert;
import org.testng.annotations.DataProvider;
import org.testng.annotations.Parameters;
import org.testng.annotations.Test;

import com.osa.base.AppContains;
import com.osa.base.Browser;
import com.osa.common.App;
import com.osa.utility.Utilities;

import osa.com.pages.HomePage;

public class HomePageTest {
//	public WebDriver dr;
//	HomePageTest hp;
//	@Test(dataProvider="loginData", dataProviderClass=Utilities.class)
//	public void verifyLogin( String username, String password, String browser) {
//		dr=Browser.openBrowser(browser);
//		dr.get(App.URL);
//		hp=new HomePage(dr);
//		hp.clickOnForumButton();
//		dr.findElement(By.id("username")).sendKeys(username);
//		dr.findElement(By.id("password")).sendKeys(password);
//		dr.findElement(By.xpath("//*[@id=\"login_button\"]")).click();
////		if(username.equals("username3")) {
////			Assert.assertEquals(1, 2);
////		}
//		dr.close();
//	}

}
